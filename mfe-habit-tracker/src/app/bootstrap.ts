// src/app/bootstrap.ts
import { defineComponent, h } from "vue";

export default defineComponent({
  name: "HabitTrackerRemote",
  render() {
    return h("div", "Habit Tracker Remote (Vue)");
  },
});
